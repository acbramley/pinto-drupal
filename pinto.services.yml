parameters:
  # List of namespaces (directories) containing Pinto enums.
  # Override with a site level services.yml.
  pinto.namespaces:
    - 'Pinto'

services:
  _defaults:
    autowire: true
    public: false

  Pinto\PintoMapping:
    class: Pinto\PintoMapping
    public: true
